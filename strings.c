


#include <stdio.h>

// STRINGS

int main()
{

    char name[20] = "sam";

    // format specifier as %s

    // printf("%s", name);
    // access any alphabet /character -> indexes

    // printf("%c \n", name[1]);
    // printf("%c \n", name[0]);
    // printf("%c \n", name[2]);

    // puting for value for a = x that show
    // modify / update the character values in string
    // name[1] = 'x';

    // printf("%s", name);

// loop over a string 
for ( int  i = 0; i < 3 ; i++)
{
    printf("%c \n", name [1]);
} 
    return 0;
}
